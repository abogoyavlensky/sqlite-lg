[tools]
# wasi-sdk provides clang + the WASI sysroot used to (re)build resources/sqlite3.wasm.
# Installed via mise's github: backend.
wasi-sdk = "wasi-sdk-33"

[tool_alias]
wasi-sdk = "github:WebAssembly/wasi-sdk"
